spring:
  application:
    name: eureka-service
  cloud:
    inetutils:
      preferredNetworks: true
    discovery:
      enabled: true
    config:
      enabled: true
      uri: http://localhost:8081